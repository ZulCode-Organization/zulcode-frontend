import { PerfilUsuario } from "@/lib/types/perfil";

export function AchievementsSection({ conquistas = [] }: { conquistas?: PerfilUsuario["conquistas"] }) {
  return <section className="rounded-[20px] border border-border bg-card p-5"><h2 className="text-lg font-black text-foreground">Conquistas</h2><p className="mt-1 text-sm text-muted-foreground">Seus emblemas e banners conquistados aparecem aqui.</p>{conquistas.length ? <div className="mt-4 grid gap-3 sm:grid-cols-2">{conquistas.map(item => <div key={item.id} className="flex items-center gap-3 rounded-2xl bg-muted/50 p-3"><span className="size-11 shrink-0" dangerouslySetInnerHTML={{ __html: item.iconSvg }} /><div><p className="font-black text-foreground">{item.title}</p><p className="text-xs text-muted-foreground">{item.description}</p></div></div>)}</div> : <div className="mt-4 rounded-2xl border border-dashed border-border px-4 py-5 text-sm text-muted-foreground">Conclua metas semanais, suba de rank ou complete uma unidade para conquistar seus primeiros emblemas.</div>}</section>;
}
